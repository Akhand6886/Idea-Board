version: '3.8'

services:
  ideaos:
    build: .
    container_name: ideaos
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    volumes:
      - ideaos_data:/app/server

volumes:
  ideaos_data:
